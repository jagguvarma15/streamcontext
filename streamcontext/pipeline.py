"""Pipeline orchestrator — wires consumer + embedder + sink. Lands in Day 3."""

from __future__ import annotations

from streamcontext.config import Settings


async def build_and_run(settings: Settings) -> None:  # pragma: no cover - stub
    raise NotImplementedError(
        "pipeline.build_and_run is implemented on Day 3 (vector sink + orchestrator)."
    )
